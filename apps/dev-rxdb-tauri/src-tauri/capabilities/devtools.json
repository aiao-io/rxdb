{
  "$schema": "../gen/schemas/desktop-schema.json",
  "description": "Capability for the dev-only rxdb-devtools window (US-905 AC#1): minimal event access so the panel can listen on the devtools:message channel. Does not inherit the main window's core:default (no window controls, no SQL/filesystem access).",
  "identifier": "devtools",
  "permissions": ["core:event:default"],
  "windows": ["rxdb-devtools"]
}
