# US-905 / wa-sqlite worker：关闭 Angular 生产构建的 chunk 再优化（rollup re-chunking）。
#
# @angular/build 22.1.x 的 optimizeChunks 只从 main 入口走图，却会删掉与主图 chunk
# 同名的 worker 图 chunk（esbuild 按内容哈希命名，两图同名同哈希），随后只补回主图
# 的 rollup 重命名产物。worker 入口（worker-*.js）不在重写范围内，仍静态
# import "./chunk-XXXX.js" 指向已删除的文件 —— wa-sqlite worker 加载即 404，app 白窗。
# 22.2 之前无修复版；官方关闭开关即 NG_BUILD_OPTIMIZE_CHUNKS=false（阈值 Infinity）。
NG_BUILD_OPTIMIZE_CHUNKS=false
