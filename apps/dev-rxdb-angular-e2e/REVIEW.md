# `dev-rxdb-angular-e2e` 代码评审

## 结论

🔴 不通过。首页导航测试已修复并通过，但 OPFS 数据完整性仍没有 E2E 门禁。

## 修复状态（2026-07-15）

- ANGULAR-E2E-002 已修复：测试真实点击 Todo 导航，并断言 URL 与目标标题。
- ANGULAR-E2E-001 未修复：仍需补 OPFS 冲突、失败回滚与刷新持久化场景。
- `pnpm nx e2e dev-rxdb-angular-e2e` 通过，103 个用例全部成功。

## 评审基线

- 基线提交：`03a46a5d5992a958c19ae33d5fed15c9c3322021`
- 评审日期：2026-07-14
- 范围：`apps/dev-rxdb-angular-e2e` 下 Playwright 用例、夹具和 Nx 配置；26 个文件
- 自动校验：此次仅进行只读代码审查，未单独运行本项目的 `e2e`、`lint` 或其他自动任务，不能据此判定通过
- 测试现状：19 个 spec/test 文件；没有 OPFS 文件管理与重命名数据完整性用例

## 问题

| ID              | 级别 | 位置                  | 问题与影响                                                                                                                                                            | 建议                                                                                                                           |
| --------------- | ---- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| ANGULAR-E2E-001 | P1   | `src/*.spec.ts`       | 整套 19 个 spec 没有 OPFS 数据完整性测试。应用中的重命名实现可以覆盖同名文件、合并目录并删除源，但 E2E 仍会全绿；最可能丢用户数据的功能没有任何发布门禁。             | 增加真实 OPFS 场景：创建源和同名目标、触发重命名、验证操作被拒绝且双方内容不变；同时覆盖嵌套目录、复制失败回滚、刷新后持久化。 |
| ANGULAR-E2E-002 | P2   | `src/home.spec.ts:13` | `should navigate to different pages from home` 只再次断言 `app-home` 可见，没有点击导航、没有检查 URL、也没有验证目标页面。测试名与行为不符，会制造不存在的路由保障。 | 点击一个真实导航入口，断言 URL 和目标页面唯一标识；若只想做首页烟测，就改名并删除“navigate”承诺。                              |

## 其余观察 / 测试缺口

- 已有加密等业务流程测试，但数据破坏风险最高的 OPFS 页面没有对等覆盖。
- 多数用例验证可见文案；对于持久化功能，还需要刷新、重开上下文后的状态断言。
- 没有把“操作失败不得改变原数据”作为共享测试契约。

## 验收条件

- 为 OPFS 重命名冲突、嵌套目录、失败回滚和刷新持久化建立 Playwright 用例，并能在缺陷实现上稳定失败。
- 首页导航测试必须发生真实点击，同时断言 URL 与目标页面；禁止保留名不副实的空壳测试。
- 修复后执行 `pnpm nx e2e dev-rxdb-angular-e2e`，并记录目标浏览器和 OPFS 支持条件。
