# `dev-rxdb-react-e2e` 代码评审

## 结论

🟢 好。缓存命中已由 Playwright 网络层独立计数，首页导航也执行真实点击并验证目标页面。

## 修复状态（2026-07-15）

- REACT-E2E-001、REACT-E2E-002 已修复。
- `pnpm nx e2e dev-rxdb-react-e2e` 通过，126 个用例全部成功。

## 评审基线

- 基线提交：`03a46a5d5992a958c19ae33d5fed15c9c3322021`
- 评审日期：2026-07-14
- 范围：`apps/dev-rxdb-react-e2e` 下 Playwright 用例、网络夹具和 Nx 配置；28 个文件
- 自动校验：此次仅进行只读代码审查，未单独运行本项目的 `e2e`、`lint` 或其他自动任务，不能据此判定通过
- 测试现状：22 个 spec/test 文件；remote-cache 的“无网络命中”没有独立观测，首页导航测试没有执行导航

## 问题

| ID            | 级别 | 位置                          | 问题与影响                                                                                                                                                            | 建议                                                                                                                          |
| ------------- | ---- | ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| REACT-E2E-001 | P2   | `src/remote-cache.spec.ts:31` | 用例通过页面自己输出的 `OPFS hit:` 和 `no network` 文案证明第二次请求没有访问网络。若实现错误地再次 fetch、同时仍打印命中文案，测试照样通过；这是自证，不是独立验证。 | 在 route stub 或 request 监听器中计数目标 URL：首次点击必须产生一次请求，第二次点击计数必须不变；同时保留缓存内容和日志断言。 |
| REACT-E2E-002 | P2   | `src/home.spec.ts:13`         | 名为 `should navigate to different pages from home` 的测试只断言首页 `h1` 可见，没有点击链接、没有检查 URL、没有验证目标页面。路由全坏它也能通过。                    | 点击稳定的导航入口，断言 URL 与目标页唯一元素；或者把用例改成诚实的首页可见性烟测。                                           |

## 其余观察 / 测试缺口

- 网络缓存测试已经具备 stub 基础设施，缺的只是独立请求计数，不需要新增复杂框架。
- 缓存正确性还应验证第一次响应内容与第二次缓存内容一致，而不只看计数和日志文案。
- 首页测试与已有 display-home 用例高度重复，当前“导航”用例没有新增保障。

## 验收条件

- remote-cache 测试必须从 Playwright 网络层证明第二次读取没有发起目标请求，并校验缓存内容一致。
- 首页导航用例必须真实点击并验证 URL 和目标页面；空壳断言应删除或改名。
- 修复后执行 `pnpm nx e2e dev-rxdb-react-e2e`，确保测试在故意恢复重复 fetch 时能够失败。
