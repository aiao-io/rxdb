/**
 * @fileoverview `@aiao/code-editor-angular` 包入口。
 *
 * 导出 Angular `CodeEditor` 组件、`CodeEditorSetup` 与 `CodeEditorTheme` 类型。
 * 与 React / Vue 绑定同名同语义，三端共用 `@aiao/code-editor` 的语言解析与无障碍工具。
 *
 * @example
 * ```ts
 * import { CodeEditor } from '@aiao/code-editor-angular';
 * ```
 *
 * @module @aiao/code-editor-angular
 */
export { CodeEditor } from './code-editor';
export type { CodeEditorSetup, CodeEditorTheme } from './code-editor';
