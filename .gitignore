node_modules/
dist/
build/
coverage/
# coverage-acceptance 的 blob 中转目录现已改由 runner 建在 os.tmpdir() 下（SQLC-039），
# 正常情况不会再出现在包内。这条规则留作兜底：runner 开跑前与 finally 都会整个 rmSync，
# 一旦哪个配置退回包内路径又被跟踪，跑一次门禁就会把它标成已删除。
.coverage-acceptance/
# rxdb-client-generator 的 generated-runtime 夹具目录（mkdtemp 建在包内，finally 里 rm）。
# 进程被 kill 或用例超时崩掉时会残留，`tmp/` / `*.tmp` 都匹配不到这个前缀。
.tmp-generated-runtime-*/
tmp/
*.log
/logs/
.env*
.DS_Store
Thumbs.db
*.tmp
*.swp
# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc
# Cargo 构建产物。crate 内 .gitignore 对 Prettier / nx format 无效，它们只读仓库根 ignore。
**/target/

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/mcp.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

.nx/cache
.nx/workspace-data
.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

.angular

vite.config.*.timestamp*
vitest.config.*.timestamp*

packages/*/test-output
packages/rxdb-model-angular/styles.css

.env

.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md
.VSCodeCounter

# Binary WASM files (downloaded at build time)
*.wasm
!packages/rxdb-adapter-miniprogram/assets/wa-sqlite.wasm

integration/*/pnpm-lock.yaml

test-output
playwright-report
/tsconfig.tsbuildinfo

# Extra patterns for workspace
benchmarks/dist/
apps/dev-rxdb-angular/dist/
apps/dev-rxdb-angular-e2e/dist/
apps/dev-rxdb-react/dist/
apps/dev-rxdb-react-e2e/dist/
apps/dev-rxdb-vue/dist/
apps/dev-rxdb-vue-e2e/dist/

*.tmp
*.swp
*.sqlite
*.db

# Playwright test output
test-output/
playwright-report/
test-results/

# Benchmark results
benchmark-results.json
**/benchmark-results.json
.playwright-mcp

# Lighthouse / performance audit artifacts
*lighthouse*.json
lighthouse-report.json

# Stray local logs and scratch files
build_output.txt
test_output.txt
test-search.js

# Search plugin benchmark outputs (generated by benchmarks/scripts/search-ci.mjs)
benchmarks/reports/rxdb-plugin-search-latest.json
benchmarks/reports/rxdb-plugin-search-*.md

__screenshots__/

tsconfig.tsbuildinfo

.claude/worktrees
.claude/settings.local.json
.nx/polygraph

.nx/migrate-runs

# HTTP demo 后端的本地 SQLite 库（US-214 AC#1）。开发数据，不进版本库；
# e2e 另用临时目录，见 apps/dev-rxdb-http-e2e。
apps/dev-rxdb-http-server/.data/
