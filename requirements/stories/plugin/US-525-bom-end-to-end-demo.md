---
id: US-525
title: BOM 端到端 demo：一份数据集走完全域
status: Backlog
priority: Low
epic: epic-009-bom-domain-model
created: 2026-09-22
updated: 2026-09-26
tags: [plugin, bom, demo, e2e, angular]
---

<!--
INVEST 检查清单:
- [ ] Independent: **不独立**，这是本故事与 epic-009 其余 18 条最大的差别——它消费全部前置，自己不产出领域能力。阶段 A 的前置是首轮切片（US-507/508/509/510 阶段 A/511），阶段 B~D 各自等对应故事
- [x] Negotiable: 产品选型、页面形态、种子行数、端口可调；「一份数据集贯穿全阶段」「每个数字并排显示手算式」「评估日是显式入参」三条不可协商——去掉任一条，demo 就退化成 18 条故事各自 AC 的重复演示
- [x] Valuable: 各故事的 AC 都是**单点断言**，而 BOM 的典型缺陷出在组合处——七步公式乘序、闭包与生效期的二次过滤、替代组加权与损耗的先后。单点全绿不蕴含组合正确，今天没有任何一处会让组合错误显形
- [x] Estimable: 一个 Angular app（展开树 + 算式面板 + 评估日切换 + 双图对比）、一份确定性种子、一个 playwright e2e app
- [x] Small: 体量偏大，按 CONVENTIONS「大故事分阶段」切 A/B/C/D 四阶段，不拆子文件；阶段 A 可独立交付并独立有价值
- [x] Testable: 15 条 AC，每条都落成一条 playwright 用例，且每条的预期结果都是**页面上看得见的具体数字或具体拒绝**
-->

# 用户故事：BOM 端到端 demo：一份数据集走完全域

## 作为/我想要/以便

**作为** 要判断这套 BOM 模型「设计对不对」的评审者与第一批接入者
**我想要** 一台电动滑板车的完整数据集在一个能一键起起来的应用里跑完——多重边、虚拟件、替代组、
联产副产、工序分摊、ECN 生效期、EBOM↔MBOM 重构、成本卷算，**同一份数据，不换样例**
**以便** 每个展开量旁边并排显示它的七步手算式，算错的时候当场看见差在第几步，
而不是拿到一个看起来正常、要到 MRP 下单才发现不对的数字

## 交付阶段

| 阶段  | 内容                                     | 关闭判据                                          | 状态 |
| :---- | :--------------------------------------- | :------------------------------------------------ | :--- |
| **A** | 首轮切片闭环：建图 → 视图 → 展开 → 反查  | AC#1～6 全绿；一条 `serve` 起来就能操作整台滑板车 | ⬜   |
| **B** | 数量的三个硬用例：替代组 / 多输出 / 工序 | AC#7～9 全绿                                      | ⬜   |
| **C** | 时间轴：评估日、ECN 影响面与整体回退     | AC#10～12 全绿                                    | ⬜   |
| **D** | 双图与卷算：EBOM↔MBOM、成本、门禁        | AC#13～15 全绿；`pnpm nx e2e dev-rxdb-bom-e2e` 绿 | ⬜   |

阶段 A 有独立价值：它是[首轮可开工切片](../../epics/epic-009-bom-domain-model.md#首轮可开工切片)的**验收手段**，
不依赖 B~D 就能被用来判断那五条故事是否真的交付了。后三个阶段各自等对应故事落地，
**全部阶段关闭后才置 `Done`**（[CONVENTIONS 大故事分阶段](../../CONVENTIONS.md#大故事分阶段不拆子故事文件硬规则)）。

## 范围边界

### In Scope

- **`apps/dev-rxdb-bom`**：Angular demo，展开树 + **算式面板** + 评估日切换 + EBOM/MBOM 双图对比
- **一份确定性种子数据集**：一台电动滑板车 `SC-100`，落
  `packages/rxdb-plugin-bom/src/testing/scooter-dataset.ts`，**demo 与 vitest 用例共用同一份**
- **算式面板**：选中任一展开行，按 [US-511](US-511-bom-quantity-semantics.md) 七步公式
  逐步列出中间值（`P` / `P'` / `G` / `G'` / `G''` / 穿透 / 换算），每步标出取自哪个字段
- **评估日（as-of date）显式入参**：页面顶部输入，驱动 [US-508](US-508-bom-view-resolution.md)
  的生效期过滤；**不读系统时钟**
- **直连写入入口**：绕过应用层校验直接写库的按钮，用来证 [US-509](US-509-bom-dag-cycle-detection.md)
  AC#5 的约束真的在存储层
- **`apps/dev-rxdb-bom-e2e`**：playwright，15 条 AC 各至少一条用例

### Out of Scope

- **可复用的三框架绑定**（→ [US-522](US-522-bom-tri-framework-ui.md)）：本故事交付的是**一个应用**，
  不是**一套绑定**。单端即可，先例是 [US-214](../adapter/US-214-http-browser-demo.md)（`status: Done`，
  只有 Angular 前端）。三端对称是 US-522 的关闭条件，不是本故事的
- **配置器界面**（US-517 的特征/选项 UI）与**实例 BOM**（US-523）——两条各自的模型先落地再谈演示
- **性能基准**：本故事的数据集按「覆盖全部硬用例」设计，不按「压出规模」设计
- **新领域能力**：本故事**不新增任何抽象**，全部行为都归属到已有的 18 条故事

## 验收标准

前置条件里的「demo 已起」统一指：`pnpm nx serve dev-rxdb-bom` 起在 `127.0.0.1:4310`，
已跑过 `seed`，评估日默认 `2026-09-22`。

### 阶段 A：首轮切片闭环

| #   | 前置条件                                          | 操作                                            | 预期结果                                                                                                                                                                            | 状态 |
| --- | ------------------------------------------------- | ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 1   | 仓库无 `apps/dev-rxdb-bom`                        | 连续执行 `nx run dev-rxdb-bom:reset seed` 两遍  | 两遍产出**逐字节相同**：`id` 由业务键确定性派生，**零随机、零 `Date.now()`**；否则 e2e 只能断言「大概有几条」（[US-214](../adapter/US-214-http-browser-demo.md) AC#6 的同一条理由） | ⬜   |
| 2   | PCBA 下同一只 10kΩ 电阻分属三组位号、`qty` 各不同 | 展开 PCBA                                       | **三行独立可见可编辑**，不按物料合并；三行的 `line_no` 与位号清单各自完整（[US-507](US-507-bom-graph-skeleton.md) AC#1）                                                            | ⬜   |
| 3   | 某行 `qty = 100`、位号清单 39 个                  | 保存                                            | 拒绝，错误消息**同时给出 39 与 100 两个数**（US-507 AC#5）；页面不自动补位号、也不把 `qty` 改成 39                                                                                  | ⬜   |
| 4   | 线束 `WH-01` 的 D 版 `default_phantom = true`     | 展开整机，选中线束下的导线行                    | 展开结果里**没有 `WH-01` 的需求行**，导线/端子/热缩管按 `G''` 作为 `P` 继续下钻；算式面板第 6 步显示 `COALESCE(l.phantom_override, r.default_phantom)` 的取值与来源修订号           | ⬜   |
| 5   | demo 已起                                         | 用**直连写入**按钮插入一条从 `SC-100` 的下级件指回 `SC-100` 的边（闭合多跳环） | 写入被**存储层**拒绝，页面显示的是数据库抛出的错误（US-509 AC#5）。对照一条：走正常表单同样被拒，但错误来自应用层——两条消息可区分，否则证不出约束下沉。自反边代替不了这条：它由 US-509 AC#2 的 CHECK 拦下，证不出可达性触发器 | ⬜   |
| 6   | 整机展开已渲染                                    | 对某颗 M3 螺钉做 where-used 反查                | 列出全部父件与**每条路径**（`line_path`），含经由虚拟件的那条；路径上的每一跳可点回展开树（[US-510](US-510-bom-multilevel-explosion.md)）                                           | ⬜   |

### 阶段 B：数量的三个硬用例

| #   | 前置条件                                                       | 操作            | 预期结果                                                                                                                                                                           | 状态 |
| --- | -------------------------------------------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 7   | 10kΩ 替代组三个供应商件 `strategy = proportional`，0.5/0.3/0.2 | 展开            | 三件按权重加权，合计等于原需求；把 0.2 那行删掉使合计 = 0.8 → 保存**拒绝且错误给出 0.8**（[US-512](US-512-bom-substitute-group.md) AC#5），不归一化                                | ⬜   |
| 8   | 外壳下料一行产出外壳件、一行边角料、一行余料回用               | 展开 + 看成本页 | 只有 `consume` 行进需求展开；`by_product` 在成本页按 `credit_price` 抵减且明细可见；`scrap_out` 既不进需求也不参与环检（[US-513](US-513-bom-coproduct-byproduct.md) 的四行消费表） | ⬜   |
| 9   | 某行分摊到工序 10 / 30，比例 6:4；工序 20 有 3% 损耗           | 展开            | 两工序各得对应量、合计**无舍入漂移**；工序 20 之后投料的行被放大 3%，工序 10 投料的行不受影响（[US-520](US-520-bom-routing-operation.md) AC#2/#3），算式面板第 5 步显示累乘因子    | ⬜   |

### 阶段 C：时间轴

| #   | 前置条件                                              | 操作                     | 预期结果                                                                                                                                            | 状态 |
| --- | ----------------------------------------------------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 10  | 同一张 BOM 有两段不重叠生效期的行                     | 评估日在两段之间来回切换 | 两次展开的行集不同且各自自洽；**页面上找不到任何读系统时钟的路径**——把机器时钟调开一天，两次展开结果不变（[US-508](US-508-bom-view-resolution.md)） | ⬜   |
| 11  | `ECN-2026-014` 改 3 张 BOM 的 5 行，生效日 2026-10-01 | 查影响面与差异           | 列出受影响整机（经 where-used 闭包）与变更前后行集对比：新增/删除/改量/改修订四类各自可见（[US-515](US-515-bom-change-management.md) AC#1/#2）      | ⬜   |
| 12  | 同一 ECN 已保存                                       | 取消该 ECN               | 5 行**整体回退**，页面刷新后不存在「部分行已生效、部分未生效」的中间态（US-515 AC#4）                                                               | ⬜   |

### 阶段 D：双图、卷算与门禁

| #   | 前置条件                                           | 操作                                                                           | 预期结果                                                                                                                                                                 | 状态 |
| --- | -------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---- |
| 13  | EBOM 一个钣金外壳，MBOM 拆成板材 + 2 个工序件      | 双图并排                                                                       | `relation = 'split'` 双向可追溯；两侧层级结构不同且各自自洽；某 EBOM 行无映射时覆盖率页报**未覆盖**，不静默算 `removed`（[US-516](US-516-ebom-mbom-mapping.md) AC#1/#6） | ⬜   |
| 14  | 整机 5 层 + 工艺路线，其中一个子件**故意不给成本** | 卷算                                                                           | 逐层明细可展开审计；缺成本的那件**明确报缺失项**，页面上该项显示为缺失而**不是 0**（[US-514](US-514-bom-cost-rollup.md) AC#6）                                           | ⬜   |
| 15  | 两个新 project 已建                                | `pnpm nx run-many -t lint test build --projects=dev-rxdb-bom,dev-rxdb-bom-e2e` | 全绿、零 ESLint 警告；15 条 AC 各至少一条 playwright 用例；两个 project 打上与既有 demo 一致的 tag                                                                       | ⬜   |

状态符号：⬜ 未开始 / ⚠️ 进行中或有保留 / ✅ 通过

## 技术笔记

**一份数据集，不是十五份样例。** 这是本故事唯一不可让步的形态要求。
epic-009 的 18 条故事各有自己的 AC，但每条都是**单点断言**：替代组加权对、损耗乘序对、
闭包与生效期二次过滤对。BOM 的实际缺陷出在这些东西**同时发生**的地方——
一只带替代组的电阻，挂在有工序损耗的 SMT 工序上，父件是虚拟线束，整机又在 ECN 生效日前后各展开一次。
每条 AC 配一个干净的孤立样例，这类组合缺陷永远照不出来。
所以种子是一台完整的滑板车 `SC-100`：位号、替代、虚拟件、多输出、工序、序列追溯、双图重构
各自落在这台车的**不同真实部位**上，而不是十五个互不相干的迷你 BOM。

**每个数字并排一条手算式。** BOM 算错不会崩——它给出一个看起来完全正常的数字，
要到 MRP 多下了 400 颗螺钉才有人发现。算式面板把 [US-511](US-511-bom-quantity-semantics.md)
七步公式的每步中间值摊开，并标出每步取自哪个字段（`h.assembly_scrap` / `l.component_scrap` /
`op.scrap` / `r.default_phantom`）。这和 [US-214](../adapter/US-214-http-browser-demo.md)
的「协议流量面板」是同一招：把**本来不可见的中间态**摆到页面上，让错误在发生处显形而不是在下游。

**评估日必须是显式入参，这是 demo 逼出来的 API 约束。** AC#10 要求把机器时钟调开一天、
展开结果不变。做不到就说明展开实现内部读了 `Date.now()`——那样的实现既不可重放（历史展开无法复算，
与 US-511「历史展开本该可复算」的立场冲突），e2e 也只能在一天之内绿。
这条约束在 18 条故事的 AC 里没有任何一条写过，是把它们放进一个能跑的应用才暴露出来的。

**AC#5 的两条错误消息必须可区分。** 走表单被拒只证明应用层有校验；
US-509 AC#5 要证的是约束**在存储层成立**。因此 demo 需要一个绕过应用层的直连写入入口，
且两条路径的错误来源在页面上分得开。没有这个入口，AC#5 在 demo 里是演不出来的——
这也是为什么它是 In Scope 的一项，而不是调试便利设施。

**与 [US-522](US-522-bom-tri-framework-ui.md) 的分工**：US-522 交付**可复用绑定**，
关闭条件是三端同 API、单端缺失即未完成；本故事交付**一个应用**，关闭条件是一份数据集
把上述断言当场演出来。两条的前置也不同——US-522 要等三框架绑定，本故事阶段 A 只等首轮切片。
demo 单端即可，先例是 US-214（`status: Done`，只有 Angular 前端）：
本仓铁律「三框架 API 必须对称」约束的是**包的导出面**，不是 demo app 的数量。

**种子落插件而非 app**：`packages/rxdb-plugin-bom/src/testing/scooter-dataset.ts`。
理由是 vitest 用例与 demo 共用同一份数据后，demo 就是那批单测的**可视化视图**——
单测断言的数字和页面上显示的数字来自同一组输入，两边对不上时先怀疑的是实现而不是样例。
放进 app 则两边各自演化，很快就不再是同一台滑板车。

## 价值待证

同 [epic-009](../../epics/epic-009-bom-domain-model.md#价值待证整个-epic)，但本故事在两点上与其余 18 条不同，
按 [CONVENTIONS 的病灶数 ≥ 抽象数](../../CONVENTIONS.md#价值待证) 必须写明：

- **它不新增抽象**。全部行为都归属到已有故事，判据分母为零——本故事永远不会因「抽象超过病灶」被拦。
- **它也不解锁 epic-009**。epic 的解锁条件是「客户 BOM 数据集，或一个**要上线的** BOM 应用」，
  demo 两者都不是。它是解锁**之后**的验收手段，不是提前启动的理由。
  把 demo 当成解锁依据就是循环论证：demo 需要插件存在，而插件正等着解锁。

## 实现文件

| 阶段 | 文件                                                      | 说明                                  |
| :--- | :-------------------------------------------------------- | :------------------------------------ |
| A    | `packages/rxdb-plugin-bom/src/testing/scooter-dataset.ts` | 待建；确定性种子，与 vitest 用例共用  |
| A    | `apps/dev-rxdb-bom/`                                      | 待建；Angular demo，展开树 + 算式面板 |
| A    | `apps/dev-rxdb-bom-e2e/`                                  | 待建；playwright                      |
| C    | `apps/dev-rxdb-bom/` 评估日与 ECN 面板                    | 待建                                  |
| D    | `apps/dev-rxdb-bom/` 双图对比与成本明细                   | 待建                                  |

## References

- [epic-009 BOM 领域模型](../../epics/epic-009-bom-domain-model.md)
- [US-214 HTTP 适配器浏览器端到端 demo](../adapter/US-214-http-browser-demo.md) — demo 故事的形态先例；单端 demo 的先例
- [US-511 展开数量正确性](US-511-bom-quantity-semantics.md) — 算式面板摊开的那七步
- [US-524 工艺路线本体](US-524-routing-master-model.md) — 前置：阶段 B 的 AC#9 用它的工序损耗，阶段 D 的 AC#14 经 US-514 以它为前置
- [US-522 三框架 BOM 编辑与展开视图](US-522-bom-tri-framework-ui.md) — 绑定与应用的分工
