---
id: US-517
title: 可配置销售 BOM：特征、选项与选择条件
status: Backlog
priority: Low
epic: epic-009-bom-domain-model
created: 2026-09-22
updated: 2026-09-22
tags: [plugin, bom, configuration]
---

# 用户故事：可配置销售 BOM：特征、选项与选择条件

## 作为/我想要/以便

**作为** 销售配置员
**我想要** 特征、选项值、选择条件与依赖约束作为独立实体，BOM 行引用选择条件
**以便** 一张 BOM 支撑整个变体族，而不是给每个变体维护一张

## 范围边界

### In Scope

- `config_char`（特征）、`config_char_value`（选项值）、`config_condition`（选择条件表达式）、`config_constraint`（依赖约束）
- `bom_line.selection_condition_id` 引用选择条件
- 给定一组配置输入，解析出入选行集
- 配置不完整或冲突时的明确报错

### Out of Scope

- **约束求解器实现**——本故事只定数据模型与求解契约，求解可外挂
- 变式定价与报价
- 配置界面（→ US-522）

## 验收标准

| #   | 前置条件                | 操作          | 预期结果                                      | 状态 |
| --- | ----------------------- | ------------- | --------------------------------------------- | ---- |
| 1   | 行挂选择条件「颜色=红」 | 选红 / 选蓝   | 该行分别入选 / 落选                           | ⬜   |
| 2   | 约束「A 与 B 互斥」     | 同时选 A 与 B | 配置被拒，错误里给出冲突项                    | ⬜   |
| 3   | 必选特征未赋值          | 展开          | 明确报「配置不完整」，**不静默产出部分结构**  | ⬜   |
| 4   | 同一 BOM 两组配置输入   | 分别展开      | 得到两份不同的有效行集，共享同一份 `bom_line` | ⬜   |
| 5   | 选择条件表达式语法错误  | 保存          | 保存期拒绝，不留到展开期才炸                  | ⬜   |

状态符号：⬜ 未开始 / ⚠️ 进行中或有保留 / ✅ 通过

## 技术笔记

**销售 BOM 不是一张图，是「结构 + 配置模型」。** 选择条件可以作为边属性挂在 `bom_line` 上，
但特征、选项值与依赖约束是独立实体集——图属性承载不了「A 与 B 互斥」这种**跨行约束**（AC#2），
它不属于任何一条边。

这是本 Epic 里唯一需要外部求解能力的故事。范围切法是：模型与契约在本故事内闭合，
求解器作为可替换实现（朴素求值足以覆盖 AC#1～#5 的场景；真实变体族的组合爆炸再谈约束传播）。

AC#3 遵守「无 fallback 兜底」：配置不完整时返回部分结构，会让下游 MRP 跑出一份看起来正常的错误需求。

## 价值待证

同 [epic-009](../../epics/epic-009-bom-domain-model.md#价值待证整个-epic)。

## 实现文件

- `packages/rxdb-plugin-bom/` — 待建；配置模型实体与选择条件解析

## References

- [epic-009 BOM 领域模型](../../epics/epic-009-bom-domain-model.md)
- [US-508 BOM 视图解析](US-508-bom-view-resolution.md) — 前置
