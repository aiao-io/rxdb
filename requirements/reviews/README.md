# AI Review 规则与记录

这个目录集中存放**给 AI 做代码 review 用的规则/检查清单**，以及 review 过程中产出的结论记录。

## 用途

- **规则文件**：告诉 AI「review 时看什么、按什么标准判」的 md 文件
- **结论记录**：某次 review 发现的问题 + 根因 + 修复方案，修复后标记解决

## 目录结构

评审报告只留**尚未处理**的条目。复核确认已修、或判定不值得做的条目直接删除——修法与判据都写在代码注释里，报告再留一份副本只会与代码漂移。整份报告清空即删文件。

| 文件                             | 说明                                | 剩余项              |
| -------------------------------- | ----------------------------------- | ------------------- |
| `README.md`                      | 本说明与状态约定                    | —                   |
| `review.template.md`             | 新建 review 记录的模板              | —                   |
| `next-11-rxdb-package-review.md` | next-11 分支 `packages/rxdb` 包评审 | 4 块 + 1 条规格决策 |

> **2026-09-25 清理（review 分支评审）**：`review-vs-main-branch-review.md` 整份删除——它评的是 `review` 分支相对 main（`b18def11`）的差异，自报「10 个提交」，对得上的是 `b18def11..4926c162`（它列出的提交信息是 `23123`，不是与之并列的 `5c07e32b` 的 `23213`）；31 条发现（3 🔴 / 12 🟡 / 16 🟢）按 HEAD `6d3d7a21` 逐条复核，没有一条原样留在报告里。**三条 🔴**：§3.2-1「API 门禁实跑 exit 1」在 HEAD **已不成立**——`node scripts/audit/api-surface.mjs --check` exit 0，SC-014 命名例外与缺的基线条目是报告范围之后的 `5c07e32b` / `54d47212` / `0b4f667a` 补上的；另两条属实，已修。**①** 物化接管的屏障事务被当 CRUD 捕获（§3.1-1）：`takeOverBranchSwitchWithMaterialization` 在 `adapter.transaction()` 事务体**末尾**自报 `branch_materialization`，登记为受信调用点 #10，`TrustedWritePrimitive` 随之多出 `'adapter.transaction'`，登记表从两个写原语扩到三个（`adapter-contract.md` §3 表格、`trusted-callsite-registry.spec.ts`、`working-tree-callsite-drift.mjs` 与它的 spec 同步改到 10 行）。声明放在末尾而不是开头，是因为挂载点 2 取声明时先问执行器，开头那张票会被 `applyPage` 里嵌套的 `executor.mergeChanges()` 先取走，屏障自己反倒落回 `crud`——判据在 `materialize-branch.ts` 调用点的注释里。红测试是 `capture.suite.ts` 的「切到 metadata-only 分支时屏障里的物化不产生单元：走真实 switchBranch，切完每条分支 entryCount 仍为 0」，六个后端都跑。**②** sync 跳过坏 id 分支后子分支 `parentId` 悬空、延迟外键在 COMMIT 时整批回滚（§3.3-1）：先算出本轮被跳过的分支，子孙（含多代）一并跳过，分叉点翻译失败的旧通道同样连带；`SyncBranchesResult` 新增 `skipReasons`（新导出类型 `SyncBranchSkipReason`：`invalid-id` / `unresolved-from-change-id` / `ancestor-skipped`，基线已补），调用方从此分得清会自愈的跳过与永久跳过。红测试是 `sync-branches.spec.ts` 的「坏分支的子孙（含多代）连带跳过」与「分叉点翻译失败的分支若带有子分支，子分支连带跳过」。**🟡 / 🟢 里属实并已修的**：count 的「回 SQL 重算」补到 REMOVE / UPDATE 两侧（§3.2-2，`handleCountUpdate` 的本地 ± 调整整段删除，`review-query.regression.spec.ts` 新增 Q8 / Q9，`merge_remove` / `merge_update` 两份 spec 按新语义重写）；merge / restore 的外层 `adapter.transaction()` 显式传 `transactionLog=false`（§3.3-2，保住逐实体的 undo 粒度、省掉一次全表触发器重建，history 与 pglite 各补一条历史分组用例）；sqlite 建表路径的裸写窗口（§3.4-1）：`createTables` 在同一个引导事务里先读真实活动分支（`readActiveBranchIdOrMain`，与 `migrateSystemSchema` 共用一份）、再按它生成建表 + 触发器 SQL，触发器从建出来那一刻就烙对分支，红测试是 `shared-version-branch.suite.ts` 的「库停在非 main 分支时，补建的表要把变更记在当前分支名下」，六个 sqlite 系后端都跑（含 tauri Rust 适配器的 `test-conformance`）——`create_tables_sql` 因此多了必填的第三个参数 `branchId`，属破坏性签名变更，与本分支 `c549d430` 把两端触发器生成函数的 `branchId` 改必填一起登记为迁移文档第 6 项（后者是报告没点名、与 §3.2-3 同类的漏登记）；`withTriggersDisabled` 的 body 抛错后照样重建触发器并原样重抛原错误，两边都失败时抛 `AggregateError`（§3.4-2）；`readCurrentBranchId` 有行却缺 id 列时抛错，不再钳成第 0 列（§3.4-3）；tauri 加密工厂迁到 `@aiao/rxdb-test/encrypted` 的共享登记（§3.5-1，`?? 0` 兜底删除，新增 `rust-adapter-factory.spec.ts` 钉「未登记的 adapter 必须抛错，不能兜底成 0」）；`registerRepositoryGenerator` 本体拒绝重名（§3.5-3，程序化调用方与 CLI 走同一道检查）；`restoreInitialRows` 在分支表被 `shouldDeleteTable` 留下时拒绝执行，且拒在任何 DELETE 之前（§3.5-5）；`parseRegistry` 校验 `intent` 必须挂在 `TrustedWriteIntent` 上（§3.5-6）。文档类：`website/docs/migration/v1.md` 新增「`0.0.25` 之后（未发布）」六项破坏性变更——§3.2-3 的三项、querycache 收窄、核心根入口不再导出九个实体内部函数（这一项顺带订正报告 §4.1「核心纯新增、零删除」，那九个是报告范围之后的 `5c07e32b` 删的），以及上面那条触发器 / 建表 SQL 生成函数的 `branchId` 必填化；`RxDB.ts` 把「同一个同步块」改成说清接收端开着事务时安装会延后到 COMMIT（§3.2-6）；`copyStaticProperties` 写明静态属性是引用共享（§3.2-7）；`plugin-tree` README 写明运行时测试跑 `nx test-browser`（§3.5-2）；`cli.interface.ts` 写明相对路径的基准——CLI 按配置文件目录，vite 插件按 `process.cwd()`（§3.5-4）。**部分属实**：§3.1-2 的页号那半属实——两路续同一份 staging 发出同一个页号时，后到的那页现在翻译成新增的 `BranchNotMaterializedReason` `'page_conflict'`，staging 照样可续（`commit.suite.ts` 新增用例）；`CommitBranchRef` 主键那半不可达，屏障的检查与写入同处一笔串行化的写事务，后到的一路在 INSERT 之前就先拿到 `StaleActiveBranchError` / `target_already_materialized`。§3.2-4 的两个数都对（表里 5 行、挂载点 4 个），缺的是解释，已补进 `capture-mount-points.ts`。§3.2-8 的不对称属实，但该改的是 UPDATE 侧：关系选项类型上是 `readonly?: never`，`EntityManager.init()` 的 `readonlyOnRelation` 规则也拒绝注册，那句 `relation.readonly` 跳过是死代码，已删，两侧各写明理由。§3.1-4 / §3.1-5 属实但不在本轮修：staging 明文 payload 在 threat-model 门禁表加一行（物化 staging 页指纹不防 payload 形态）；FNV-1a/32 的 50% 生日碰撞点订正为约 7.7 万条（报告写 6.5 万），且今天没有生产代码比较 `fingerprint`（`findCommitConflict` 比的是三个 revision），换 SHA-256 由 US-308 定。**转 roadmap 五行**（[status-overview](../status-overview.md) 的不阻塞顺延项由六项改为十一项）：上面这两条，加上 §3.2-5 活 count 的刷新成本（`merge_create.ts` 的 count 分支补注释）、§3.3-5 同步跳过没有持久标记（先靠 `skipReasons` 让调用方区分）、§3.4-4 supabase 只把 main 的变更落实体表（`isActiveBranch` 改名 `isMainBranch`，注释指向 roadmap）。**证伪两条**：§3.1-3（屏障不复查 local / remote 的 ABA）——分页期间删分支会经 `removeBranchCommitRows` 把 staging 连同分页原子删掉，屏障拿到的是 `stage_missing`；重建的本地分支又由 `createBranchCommitRows` 立即得到自己的 ref，不存在「空 HEAD 占位 ref 被就地接管」的状态，T114 的 `remove-branch-aba.spec.ts`「materialization attempt 与它的分页一并消失」已钉住。§3.1-6 不是笔误：「两条分支」指续用与新建这两条代码路径。**属实但不改**：§3.3-3（适配器没调 `prepare` 的违约只能在 COMMIT 之后发现——要发现它本来就得等适配器返回）与 §3.3-4（`switch_branch_actions()` 先算、被拒时只读不写；采样窗口不是本轮引入的），两条的取舍都已写在 `VersionManager.ts` 的注释里。**无害 / 不可达**：§3.4-5 的多 active 行——迁移路径上由紧随其后的 `ensureBranchActiveKey` 抛 `AmbiguousActiveBranchError` 整段回滚，迁移之后由 `activeKey` 唯一索引压到至多一行，判据写进了 `readActiveBranchIdOrMain` 的 TSDoc；§3.4-6——带初始行的调用只发生在新库路径，此时当前分支必为 main。§4.3 第 2 条（pglite 建表不重挂触发器）在 HEAD 已修：`RxDBAdapterPGlite.createTables` 在同一事务里按当前分支重挂。报告 §6 的「提交信息全是占位符」不在代码评审范围，合并时由维护者决定要不要整理成可读的提交序列。§3.4-1 的红测试首跑时 COMMIT 报外键失败，根因不在实现而在测试的清库链路：`cleanup_db` 背后的共享工具 `cleanupSqliteTestAdapter` 撤触发器、按 main 重挂都只按 `config.entities` 枚举实体，够不到那张临时补建的表，它还活着的删除触发器在逐表清空时向 `rxdb$rxdb_change` 写出一条悬空的变更行；用例在 afterEach 里先 DROP 这张表绕开（DROP TABLE 的隐式 DELETE 不触发触发器，判据在用例注释里），共享清库工具的这个缺口不在本轮修。顺带校正三处指向本轮改动文件的行号锚点：US-018 指向 `RxDBClientGenerator.ts` 的两处（L499 / L537），`release-plan.md` 指向 sqlite-core 版本水位判据调用点的一处（L679，HEAD 时就已漂到 `createTables` 的右括号上）。
> **2026-09-24 清理（收口轮）**：`next-0912-branch-review.md` 整份删除——十三行 §3 条目与 §4.2 的三条跨行决策全部收口，报告里再没有一行未处理项。**本轮唯一的代码改动**是 [`scripts/audit/working-tree-callsite-drift.mjs`](../../scripts/audit/working-tree-callsite-drift.mjs)：扫描器那三张硬编码词表（受信写原语的宿主、方法，以及批量写方法名）现在每次跑都先与真实类型对一遍——`parsePrimitiveVocabulary` 按 AST 读 `TrustedWritePrimitive` 的联合成员，`parseBulkWriteMethods` 读 `bulk-write-gate.ts` 的 `METHOD_NAMES`，`assertScannerVocabulary` 按**集合**双向比、对不上就抛，且跑在扫描**之前**。原问题不是「写成了字面量」而是「字面量没钉住」：核心给联合加一个新宿主或新方法而词表没跟上，扫描器不会报错——它会安静地一处都扫不到，然后打印一行 ✅。红测试是 `working-tree-callsite-drift.spec.mjs` 新增的「扫描词表与真实类型的绑定」一组（去掉校验即红），词表所以只能是字面量的理由（`CALL_PATTERN` 是模块级正则，构造它时没有读文件的时机）写在常量的 `@remarks` 里。**其余条目按三类收口，判据一律落在代码里、报告不留副本**：（a）8 处写进对应文件的 `@fileoverview` / `@remarks`——提交图守卫的水位、`list-commits` 的按分支派生结构、`status.ts` 的聚合、`RxDB.ts` 的建表探测、`activeKey` 那第二份拷贝、两个 `switch-result.utils.ts`、两处 `ensureBranchActiveKey`、两个 `switch_branch.ts`；（b）「它挡着什么、谁来定」登记进 [roadmap 的「epic-006 评审顺延的架构项」](../roadmap.md#epic-006-评审顺延的架构项) 六行，并据此把 [status-overview.md](../status-overview.md) 里「分支评审的架构级 P1」那条不写 Done 的理由清零；（c）四条经复核**证伪**而删除：§3.4 的 `'$'` 拼物理表名（适配器早已暴露 `RxDBAdapterLocalBase.physicalTableNames()`，插件也早已改成问它）、§3.4 的「两端 `switch_branch` 用不同机制修同一根因」（两端现都是两条语句 + `RETURNING`，机制已一致，剩下的只是语句收集协议）、§3.2 的「`---STATEMENT_SEPARATOR---` 两端协议不同」里把它当成 sqlite 的那半（那是 pglite 侧独有的哨兵，sqlite 侧逐条执行，不存在「统一」这个动作）、§3.4 的「注册表被字段顺序正则二次解析」（`parseRegistry` 早已是 AST 解析）。另订正一条判据：§3.3 的水位剪枝原记「本节最高收益项」，实际它**违反 FR-051 的 MUST（遍历完整可达父链）与 SC-013 的验收**，是一次规格变更而不是性能重构，必须先改 spec——这一条写在 `commit-graph-guard.ts`，也是它在 roadmap 里排第一行的原因。§4.2 第 3 条（词法门禁是否改 AST）**判定不做**，收益只多「类型层面确认接收者的类」，代价是给一条秒级门禁配一份覆盖全仓的类型化 Program；重提门槛（先给出一处「现有词法判据放过了、AST 能拦住」的实例）写在脚本的 `@fileoverview`。
> **2026-09-24 清理（max 轮收口）**：`next-0912-branch-review-max.md` 整份删除——最后三条全部落地，剩下的那段 `normalizeCreateEntity` 与 `next-0912-branch-review.md` §3.1 的第二行是同一条，不再留第二份副本。本轮落地：**①** `RxDB.#ensureEntityTables` 摘系统表改读 `this.systemEntities`（本实例的清单）而不是模块级的 `isSystemEntity()`——后者按 `namespace:name` 在跨实例共享的注册表里比，同进程另一个库 `use()` 过的插件会让本库一个撞身份的接入方实体被静默跳过建表，首次查询报 `no such table`；判据在 `RxDB.ts` 的 `#ensureEntityTables` TSDoc，红测试是 `plugin-system-contribution.spec.ts` 的「接入方实体的身份撞上别的库登记过的系统表，照样要建出来」。**②** `StaleActiveBranchError.expected` 换成新的 `ExpectedActiveBranch`（`branchId: string | null`）：`activationRevision` 是**库级单行**、每次成功切换 +1，代际号本身就钉死了激活态，所以经 `WorkingTreeSwitchBranchOptions` 进来的调用方只断言了代际时，`branchId` 诚实地填 `null`、消息也随之换成「断言 activation revision 为 N」——原先拿当前分支 id 顶替，报出的是一个从未存在过的 token。**核心公开面没动**：`RxDBBranchSwitchPreconditions` 仍是「不多不少那两个字段」。**③** `rxdb-devtools` 的 `RXDB_EVENT_SUBSCRIPTIONS` 补上 `CAPABILITY_ENABLED`——那份 `satisfies Record<keyof RxDBEventMap, boolean>` 的编译期契约在 `CAPABILITY_ENABLED_EVENT` 进 `RxDBEventMap` 后就红了（TS1360 / TS7053），而 `nx build` 的 dts 步骤不让它失败，于是一直没人看见；electron demo 的事件探针与其 e2e 的 `EXPECTED_EVENT_TYPES` 同步从 25 类补到 26 类。顺带把全仓 `typecheck` 跑绿：`vitest` 走 esbuild 不做类型检查，四份 spec 的类型错误（受信写并发用例的 `changeId`、物化接管用例的 `localAdapterIfConnected` 与 `CommitGraphCorruptionReason`、跨连接用例调 `IRepository` 上没有的 `createEntityRef`、pglite 变更管线替身）只有 `tsc --build` 看得见。
> **2026-09-23 清理**：`RV-013` 与 `RV-014` 两份整份删除——删除方向已全部落地。`RxDBAdapterSqliteBase` 与 `RxDBAdapterPGlite` 的 `localRxDBBranch()` / `localRxDBChange()`、`RxDBAdapterPGlite.createBranch()` 与 `src/version/create_branch.ts` 一并删除，调用点改显式 `getRepository(...)`（sqlite-core 侧不需要 cast：它的 `getRepository` 默认泛型就是 `SqliteRepository<T>`；pglite 侧必须写显式泛型，因为默认的 `IRepository` 上没有 `findAll`——RV-013 这两处的建议写法都不准）。**这是破坏性变更**，影响面不止定义所在的两个包：继承 `RxDBAdapterSqliteBase` 的 `RxDBAdapterElectron` / `RxDBAdapterTauri` / `RxDBAdapterSqlite`（官方与 sqlite-wasm）/ `RxDBAdapterWaSqlite` / `RxDBAdapterSqliteai` / `RxDBAdapterWaSqliteMiniProgram` 一起失去这两个方法，子路径 `@aiao/rxdb-adapter-electron/pglite` 的 `RxDBAdapterElectronPGlite` 还一并失去 `createBranch()`；替代入口是事务外 `getLocalSystemRepositories(rxdb)`、事务内 `executor.getRepository(E)`。顺带订正 RV-013 的三处判错（RV-014 已独立查到同样三处）：`version/create_branch.spec.ts` 测的是 history 插件活路径**不能删**；`RxDBAdapterPGlite.residual.unit.spec.ts` 那个用例还直接调 `createBranch()`，只删两行断言会留编译错误，须整体删；`rxdb_adapter_create_branch.spec.ts` 里那条走 `adapter.createBranch('*active*')` 的哨兵用例 RV-013 完全没列到（`assertUsableBranchId` 的覆盖仍在 `rxdb` 的 `active-branch-guard.spec.ts` 与 `rxdb-plugin-history` 的 `create-branch.spec.ts`）。另按 RV-014 把 `RxDBAdapterPGlite.mock-residual.spec.ts` 里那四条纯变更管线用例（generation barrier、长通知链、两条超时诊断）迁到新增的 `change-pipeline.spec.ts`，直接测 `flushPendingChangePipeline()`——原来它们靠 `createBranch()` 驱动，轮次断言里混着「入口自己那一轮」，迁走后回到管线本身的 2 与 7；适配器测试只留 `switchBranch()` 的入口级冲刷与事件抑制。最后订正 RV-013 高估的收益：删掉一行别名并不能从机制上堵住事务体内误写 `adapter.getRepository(RxDBBranch)`，`getRepository` 仍是公开抽象方法，实际收益是删掉冗余且误导的公开捷径与现存的错误样本。基线未动（`requirements/api-baseline/*.json` 只记顶层导出的 `{name, kind}`，类成员不在其中）。
> **2026-09-23 清理**：`RV-015` 的判据 1（CLI 加载插件生成器的缝）已落地并删除该节——配置项 `repositoryGenerators`（`<模块>#<导出名>`，按最后一个 `#` 切分以容纳 Node `imports` 子路径）、jiti 装载与四类拒绝（缺导出 / 非类 / 形状不符 / 重名），以及 `IRepositoryGenerator.abstractEntityMetadata`——抽象基类的装饰器实参是常量标识符、静态求值取不到值，元数据必须由生成器随身携带回填，这正是原先要反向依赖插件包的原因。`GRAPH_ENTITY_BASE_OPTIONS` 同时搬进零依赖的 `constants.ts`，`/generator` 子路径不再连带 rxjs 与装饰器求值。判据落在 `repository-generators.ts` / `RepositoryGenerator.interface.ts` 的 TSDoc 与 `repository-generators.spec.ts` 的用例里。顺带订正原判据的措辞：未声明生成器不是「不含图方法」而是 fail-closed 直接报错（继承插件基类的实体在分析阶段失败，只写 `repository` 名的在派发阶段失败），没有静默少生成的中间态。判据 2（`rxdb-client-generator → @aiao/rxdb-plugin-tree` 反向依赖）当时留给独立一轮，已在下面那条「RV-015 收口」里完成。
> **2026-09-23 清理**：`002-rxdb-model-port-branch-review.md` 整份删除——两条 P2 已收口：E2E 一条按「真实行为断言」补到三端对称（undo/redo 走按钮与 Ctrl+Shift+Z、筛选条件命中/不命中/重置的计数与空态），顺带修掉了 `41ce2181` 在 Angular / React 筛选面板上引入的 `aria-hidden="true"`（判据写在三端 entity-list 的面板注释与 `entity-list.real.spec.*` 的「筛选弹层的可聚焦控件必须留在无障碍树内」用例里）；coverage baseline 一条判定**不改**——`scripts/audit/coverage-check.mjs` 里 baseline 是「上次实测值」的趋势记录，硬门禁是固定阈值（核心包 90 / 其余 80），当前全部包都在阈值之上，按报告要求写回旧数字等于往实测文件里塞假数据。行内编辑 / 详情 Tab / 主题切换仍无 E2E：表格由 VTable 渲染在 canvas 上，行与行内编辑没有任何 DOM 抓手。
> **2026-09-23 清理**：`2026-09-18-rxdb-core-review.md` 与配套 probes 整份删除——两条 P2 已修（游标页 CREATE 合并按翻页方向裁到 limit；count 的 CREATE 不再本地加法而是回 SQL 重数），判据落在 `packages/rxdb/src/query/merge_create.ts` 的注释与 `review-query.regression.spec.ts` 的 Q4 / Q5 用例里。
> **2026-09-22 清理**：按上面的约定重扫全目录，删掉了已完成与判定不改的条目——`RV-012`（`RxDBBranch` 去树化，已 Resolved）、`RV-016`（repository `mergeOperations`，自身判据「`packages/**` 零命中」已满足）两份文件整份删除；各整分支报告里的「证伪项 / REFUTED」「❌ 不值得做」「误报订正」小节与行一并删除（判据均已落在代码注释与 TSDoc 里，报告不再留副本）。
> **2026-09-23 清理**：按约定重扫 `next-0912-branch-review.md` 与 `next-0912-branch-review-max.md`，删掉已修与判定不成立的条目，只留架构级与卡在前置决策上的。本轮落地的修复：`uninstallWorkingTreeCapture` 去掉那个「SAVED 查不到就焊上 bound 原语」的兜底参数（它恰好做了自己 docstring 警告的那件事），连带删掉 `rxdb-adapter.ts` 里随之失效的 `#rawWritePrimitives` 字段；`readBranchEntries` 由 `commit-command.ts` / `branch-commit-rows.ts` 两份合到 `capture-runtime.ts` 一份（id-asc 顺序是内容指纹的输入，分家就指纹分叉）；`commit-error-codes.spec.ts` 删掉那条「数组与 `Object.values` 互为全集」的同义反复；三端 a11y spec 的首次可见耗时不再把空串折成 `0 ms`（`Number('')` 是 0 而 0 有限，元素挂上但一个字没渲染时原断言照样绿）；`commit.suite.ts` 的 discard 版本号改从丢弃前的值推（原来拿返回值和事后重读比，是同一次写的两个出口，删掉 `+ 1` 两边一起停在原地也绿）；`metadata-only-branch-switch.spec.ts` 的激活行改现读（`removeMany` + `saveMany` 换行后种子实例照样绿）；`capability-enable.spec.ts` 的 `find` mock 开始认 `where`（原来无视条件恒返回种子行，读路径字段写错也绿）；Vue 的 `use-working-tree.spec.ts` 补 `afterEach` 卸载（~40 个组件原本挂满整个文件）；React 侧补「十二个方法引用跨 render 稳定」的用例（该契约写在文档里但没有任何测试钉住，丢掉 `useMemo` 全 spec 绿而消费者 effect 死循环）；`bench-working-tree` 接进 CI 的 `benchmark` job（与 `search-ci` 同 job 走 `nx run-many`，它是契约指定的普通 PR CI 唯一硬门禁），并清掉 `working-tree-reference.json` 里「待静默后复冻」的过期文案。判定**不成立**而删除的一条：`WorkingTreeRestoreSession` 的 `'conflicted'` 被报成死值，实际三端 spec、三端 README 与 `use-working-tree.ts` 的 TSDoc 都在用。
> **2026-09-23 清理（效率轮）**：`next-0912-branch-review-max.md` 的 §3 整节删除——四条全部落地。提交图守卫改成逐层取 ChangeSet（一次 `in` 一层，原来每个 commit 一条 `=`；分桶不用 `?? []` 兜底，而是先按本层 id 铺空桶、取不到键就抛，否则一条写坏的 `where` 会伪装成「这个 commit 没有变更集」，判据与红测试在 `commit-graph-guard.ts` 的 TSDoc 与 `corruption-guard.spec.ts` 的「同层的 ChangeSet 合成一次批量查」一组）；捕获热路径改成按批取（`createWorkingTreeCaptureBatch` + `readBranchEntries`，回归用例在 `capture-batch-queries.spec.ts`）；启用态的 raw-write context 改为装载那一刻建一次而不是每次取值新建；三端 `use-working-tree.spec` 的 ~150 行夹具（载荷构造器 + 两个门面的桩）下沉到 `@aiao/rxdb-plugin-working-tree/testing`，三份 spec 各自只留容器形态与挂载方式（Angular 的 `TestBed` / React 的 `renderHook` 与跨 render 稳定性 / Vue 的 `mount` 与显式卸载）。`next-0912-branch-review.md` §3.3 同步删掉两条已修的捕获热路径条目与 `write-commit.ts:374`（同事务重读，而那次重读正是 CAS 父 id 的来源，判定不值得改），并订正两条写错了判据的：`list-commits.ts` 那条原写「改动小、收益直接」，实际同层批量早就在了、剩下的每层往返是 BFS 本身（`Commit` 上没有 `branchId` 列），与守卫水位是同一件事；`status.ts` 那条原写「取决于 hook 调用频次」，实际挡住它的是 `IRepository` 根本没有聚合能力，属 §4.2 第 2 条的公开面决策。
> **2026-09-23 清理（兜底轮）**：清掉两个 SQL 适配器里变更触发器的分支兜底——`rxdb-adapter-sqlite-core` 的 `generate_table_trigger_sql` 与 `rxdb-adapter-pglite` 的 `generate_trigger_sql`，原先 `branchId` 可缺省、缺省回落 `'main'`。这个值被硬编码进触发器的 `INSERT ... VALUES`，决定该表此后每一次写入记在哪条分支名下；生成器替调用方填默认值等于在「这条历史算谁的」上替人做主，而做错了不报错。现改为必填（类型必填 + 运行期缺省即抛，挡绕过类型的 JS 调用方），三个原本不传的调用点（两个适配器的 `create_tables_sql`、sqlite 的 `migrateSystemSchema`）各自在注释里写出写的是哪条分支、为什么、窗口在哪。红测试在两个包的触发器 spec（「缺 branchId 时报错，而不是静默回落 main」）。`next-0912-branch-review.md` §2 的对应行只留行为那一半（迁移中途该不该读活动分支），并新增一行本轮顺带查出来的：pglite 侧**没有** sqlite 那样的「每个默认事务按真实分支重建触发器」自愈，所以在非 main 分支上补建的实体表会一直把变更记到 `main`，直到下一次切分支。
> **2026-09-23 清理（RV-015 收口）**：`RV-015-cli-plugin-generator-seam.md` 整份删除——判据 「`pnpm nx graph` 里 `rxdb-client-generator` 不再指向任何 `rxdb-plugin-*`」已满足（现只剩 `rxdb` / `utils` 两条边）。`TreeRepositoryGenerator` 连同 `TREE_ADJACENCY_LIST_ENTITY_BASE_OPTIONS` 搬进 `@aiao/rxdb-plugin-tree/generator`，按 `GraphRepositoryGenerator` 的形状带上 `abstractEntityMetadata`（`TreeAdjacencyListEntityBase` 与 `@TreeEntity` 给手写实体的别名 `TreeEntityBase` 共用同一份声明）；生成器包删掉树条目、`BUILTIN_METADATA_RESOLVERS` 只剩 `@aiao/rxdb` 一条、`workspace:*` 依赖与 rolldown external 里的树条目一并清掉。**这是破坏性变更**：`@TreeEntity` 用户必须在 `rxdb.config.ts` 里加 `repositoryGenerators: ['@aiao/rxdb-plugin-tree/generator#TreeRepositoryGenerator']`，否则 CLI 在派发阶段 fail-closed 报错（不是静默少生成）；仓内两个调用点（`packages/rxdb-test/rxdb.config.ts`、`apps/dev-rxdb-angular` 的生成器演示页）已同步。测试随生成器搬家：树的构建期用例落到 `rxdb-plugin-tree` 新增的 node 趟（`coverage` target，`VITEST_BROWSER=false`，只算 `src/generator/**`），原有 17 份运行时 spec 改名 `*.browser.spec.ts` 归 `test-browser` 趟，两趟 include 互斥、收尾合并回覆盖率闸读的目录。顺带修掉一个真 bug：`rxdb-client-generator` 的 rolldown external 逐条枚举 `node:` 内建，漏掉的 `node:os` 被打成空 stub，`./testing` 子路径一跑就是 `tmpdir is not a function`——改成 `/^node:/u` 整体外置。仍是字符串约定、**未**解耦的三处（有意保留，解耦需要另一条配置缝）：`entity-properties.ts` 把 `'TreeRepository'` 映射到 `features.tree`、`analyze-file.ts` 的 `ENTITY_DECORATOR_PACKAGES` 与 `case 'TreeEntity'`、`known-repository-generators.ts` 的报错提示表——它们只认字符串，不产生构建依赖，因此不影响上面的判据。
> **2026-09-23 清理（next-0915 收口）**：`next-0915-branch-review.md` 整份删除——4 条 P2 全部收口。两条已修：（1）`fillDefaultValue` 的时刻作用域改栈式进出（保存/恢复调用方的那一刻，而不是退出时一律清空）——默认值工厂同步 `new` 另一个实体会重入本函数，内层的 `finally` 原先把外层那一刻也清掉，外层剩余字段于是掉回读当下时钟，`createdAt === updatedAt` 随内层耗时随机失效；判据在 `entity.utils.ts` 的 TSDoc 与 `entity.utils.spec.ts` 的「默认值工厂里嵌套建实体，外层剩余字段仍拿外层那一刻」（去掉修复后实测红）。（2）`@aiao/rxdb-plugin-querycache` 的桶由 5 条 `export *` 收成「插件 + `QueryCacheEngine`」，14 个导出降到 4 —— 另外 10 个（引擎工厂、primary 仓储与其工厂、能力断言、同步备忘与指纹、默认 stale 常量与三个随行类型）包外零消费、包内全走相对路径，摆在公开面等于在 1.0 前替用户冻结只会随读路径重构而改的内部形状；`QueryCacheEngine` 保留为**类**导出，因为 `rxdb-adapter-supabase` 的错误契约测试要 `new` 真的出来。基线已按 `--update` 重生成，**这是破坏性 API 变化**，但四个插件包均未发布到 npm，无下游需迁移。判定**不值得做**的两条：（3）多个新包的测试夹具逐字重复（`private-symbols` / `reachability` / `fake-table-ref` / `test-entities`）——唯一的现成去处 `@aiao/rxdb-test` 是发布包且公开面被 `verify-public-contract.mjs` 逐条冻结，把探核心私有 `Symbol.for` 槽位的夹具搬进去，等于把内部探针写成 npm 公开 API，正是上面第 2 条要避免的事；换 `modules/` 也不行，插件包的 vite 配置一律无 `vite-tsconfig-paths`，运行期解析不到。且「副本静默腐化」不成立：这些文件都 import 核心真实符号或用全局 `Symbol.for` 键，核心一改立刻编译红。判据写在 `packages/rxdb-test/README.md` 的「什么不搬进来」与九份副本的文件头。（4）`examples/angular-todo` 未迁到 `@aiao/rxdb-plugin-history`——**做不到**：该插件没发到 npm，而示例按约定只装已发布版本。顺带订正报告的事实错误：它说「示例升到 0.0.25 即断」，实际 `"^0.0.24"` 在 0.x 下就是 `>=0.0.24 <0.0.25`（升不上去），且已发布的 `@aiao/rxdb@0.0.25` 产物里 `versionManager` 仍在。触发条件与三步升级清单写在 `examples/README.md` 的「插件拆分（US-025）与示例的滞后」和 `todo.page.ts` 的调用点注释里。
> **2026-09-24 清理（跨连接轮）**：`next-0912-branch-review.md` 的 3 条 P1 与 `-max.md` 的 D-1 全部落地并删除，同时新增 [`specs/001-working-tree-commits/threat-model.md`](../../specs/001-working-tree-commits/threat-model.md) 把两份报告里 §4.2 的三条前置决策一次性定死（跨连接**在**模型内；第三方适配器不是威胁方而是实现者；`@aiao/rxdb-test` 的边界按「可共享的进 rxdb-test，绑基类的留在各包」划）——判据写在该文件里，报告只留指针。本轮落地：跨连接能力传播改走 `CAPABILITY_ENABLED_EVENT` + 网关广播 + 广播够不着时的自愈（原先能力位以「实例是否挂了钩子」代理且 fail-open，旧连接的写静默绕过捕获）；`advanceActivationRevision` 进切换事务（A→B→A 不再能重用旧凭据）；切换前置条件随新的 `prepare` 回调进最终事务（原来前置校验与写入分属两个事务，中间那道缝里别人能切走）。`allocateBranchGeneration` 的读—改—写换成库内 `SET seq = seq + 1` 再把号读回来：加法交给库做，行锁才是决定谁先谁后的那个东西；读回来也必须走原始语句而不是仓库——仓库交出的是身份映射里的实体，它看不见上一条原始 UPDATE，回填时反倒把刚写的值当成「本地未保存的编辑」保护下来（这一条在六个后端的一致性套件上真红过，而单测那时全绿，所以 `activation-state.spec.ts` 里现在钉的是 `trace === ['query','query']`，顺序与通道一起钉）。raw 判定的「受信 intent 豁免」连 `RawWriteJudgmentContext` 上的槽位一起删除：`TRUSTED_CALLSITE_REGISTRY` 9 个条目全走带类型的写原语，一个 raw 调用点都没有，那一步在生产里永远取不到真值——却是整条防线上唯一无条件放行的一步。核心把捕获原语清单导出为**值**（`WORKING_TREE_CAPTURE_MOUNT_POINTS`），插件的 `BulkWriteOperation` 改成 `InterceptedBulkWrite` 的别名，于是核心加第四个批量写原语时插件那三张 `Record` 的穷尽性检查当场变红（原先各写一遍，漏改零编译错误、门禁静默对新方法按「不认识」处理）。测试侧按新定的 rxdb-test 边界收口：查询计数登记表下沉到 `@aiao/rxdb-test/encrypted`，读不到就抛而不是 `?? 0`（那个兜底会把整批「加密列泄漏必须拦在 SQL 生成之前」的用例变成 `0 === 0` 的空断言）；`cloneEntityClasses` 两份副本删成对核心 `@aiao/rxdb/testing` 的转出口——它们都按 `symbol.description === 'ɵMetadata'` 找槽位，而核心的描述是 `'@aiao/rxdb/ɵMetadata'`，那段元数据隔离一直是死代码；pglite 的 `cleanup_db` 改用 `systemContributions.createInitialRows` 把插件单例行补回来（位置卡在触发器重挂之前，否则清理动作自己会在下一个用例的 undo 栈里留一格）。
> 最近一次整分支复核：2026-09-25，对象是 `review` 分支相对 main（`b18def11`）的差异（报告自报范围 `b18def11..4926c162`，复核的证据行号基于 HEAD `6d3d7a21`）；报告原结论里 §3.2-1 在 HEAD 已不成立、另两条 🔴 已修，**报告本身已按上面的「review 分支评审」条目整份删除**——顺延的五项转入 [roadmap](../roadmap.md#epic-006-评审顺延的架构项)。
> 2026-09-11 的全量复核已清空 `requirements-incomplete-stories-review.md`；更早的
> `next-1123-branch-review.md`、`next-0831-branch-review.md` 与 `next-11-rxdb-adapter-tauri-review.md` 同样已删除。

## 状态约定

见 [../CONVENTIONS.md](../CONVENTIONS.md#状态定义)。

## 工作流

1. AI review 发现问题 → 从 `review.template.md` 复制出 `RV-XXX-描述.md`，`status: Open`
2. 开 PR 修复 → 在 `pr` 字段记录 PR 链接
3. PR 合并、修复完成 → `status: Resolved`，补 `updated` 日期

## 命名规范

见 [../CONVENTIONS.md](../CONVENTIONS.md#命名规范)。`RV-XXX-描述.md`，编号 `RV-001` 起递增。
例外：整分支 / 整包评审报告（如 `next-0831-branch-review.md`）结论一次性给出、没有 Open/Resolved 生命周期，不占用 RV 编号；它们的「状态」体现为文件里还剩几条。
