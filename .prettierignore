# Dependencies / lockfiles
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build / test artifacts
dist/
build/
coverage/
tmp/
out-tsc/
test-output/
playwright-report/
.env*

# Cargo 构建产物（crate 内 .gitignore 对 Prettier / nx format 无效）
**/target/

# Docker volumes config - double quotes are required in YAML for variable substitution
docker/

# Instruction/Memory markdown files (preserve manual formatting)
.github/**/*.md
.specify/**/*.md

/.angular
/.ng-doc
/.yarn

/.nx/cache
/.nx/workspace-data
__*__.*
# 逐字节快照：内容由 `JSON.stringify(x, null, 2)` 生成，Prettier 一格式化就与断言不符。
# 上面的 `__*__.*` 匹配的是「名字里带点」的文件，套不到 `__snapshots__` 这个目录名。
**/__snapshots__/
**/ios/App/**

*.d.ts

.agents/
.angular/
.claude/
.codex/
.cursor/
.git/
.specify/
