[package]
name = "git-stats"
version = "0.1.0"
edition = "2021"
# human_format 1.2.1（gix 默认 comfort 特性拉进来）需要 1.88 的 if-let chain
rust-version = "1.88"

[dependencies]
rayon = "1.10"
colored = "2.1"
gix = { version = "0.86.0", features = ["blame"] }
